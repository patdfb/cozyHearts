version: 1
backend:
  phases:
    build:
      commands:
        - npm install
frontend:
  phases:
    build:
      commands:
        - echo "No frontend build"
  artifacts:
    baseDirectory: .
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
